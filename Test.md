# テスト
## ブラウザとWebサーバ
ブラウザはWebサーバに<mark>リクエスト</mark>を送って、実行結果を<mark>レスポンス</mark>として受け取ります。

## PHPとは・・・
### プログラム？ スクリプト？
サーバサイドで用いられるスクリプト言語

### 拡張子や記述方法に関して
PHPスクリプトは、「.php」ファイルの中に、<?phpと?>で囲んで記述します。これらをPHPタグと呼称します。<?phpは開始タグ、?>は終了タグと呼びます。開始タグと終了タグの間に、スクリプトの内容を書きます。

## PHPの命令
### 変数の命名ルール
1. 変数名の前にドル($)記号を付ける
2. 1文字目は英字またはアンダースコア(_)を使う
3. ２文字目以降は英字、数字、アンダースコアのいずれかを使う
4. 英字の大文字と小文字は区別される

### require,echo
#### echo
<mark>echo</mark>はメッセージを出力する際に使用するPHPの命令です。

#### require
別のファイルに記述されたスクリプトを、読み込んで実行するための機能がPHPの<mark>require</mark>文です。

### リクエストパラメータ
フォームで入力された内容は、リクエストパラメータとしてWebサーバに送信されます。リクエストパラメータとは、Webサーバに対してリクエストを行う際に、同時に送信する付加的な情報のことです。

#### リクエストパラメータの取得方法
\$_REQUEST[\取得したい値]で取得できる。$_REQUESTは配列

###　紹介されている関数について
`htmlspecialchars`
htmlspecialchars関数は、<mark>HTMLにおいて特別な働きを持つ文字について、特別な働きを失わせる関数</mark>です。

`isset`
isset関数は、変数に値が代入されていて、かつ値がnullではないときに、trueを返します。

`array`
array関数は、配列を作成することができます。変数=$[]で記述する形は、このarray関数を短縮した構文です。

`date`
date関数は現在の日時を取得し、フォーマットで指定した形式に基づいて整形し、文字列として返します。

`rand`
rand関数は、ランダムな数を取得するために用意されています。引数を指定せずにrand関数を呼び出すと、0以上、乱数の最大値以下の乱数を返します。

`getrandmax`
getrandmax関数は乱数の最大値を取得することができます。

`preg_match`
preg_match関数は正規表現によるパターンマッチングが行えます。引数に指定したパターンに入力文字列がマッチすると、preg_match関数は「１」を返します。マッチしなかった場合には、「０」を返します。

`mb_convert_kana`
mb_covert_kana関数は、PHPのマルチバイト文字列関数の一種です。マルチバイト文字列関数とは、日本語の文字など、コンピュータ内部で複数のバイトを使って表現する文字列に対して、いろいろな機能を提供する関数です。マルチバイト文字列関数は、関数名の先頭に「mb」が付いています。
mb_convert_kana関数を使うと半角カタカナを全角カタカナに変換できます。

`file_exists`
file_exists関数は、<mark>指定したファイルが存在する場合にはtrue、存在しない場合にはfalseを返します</mark>

`file_put_contents`
file_put_contents関数は、指定した文字列を、指定したファイルに書き込みます。
この関数は、書き込みを行うファイルが存在しない場合には、新規にファイルを作成します。存在する場合には、既存のファイルを上書きします。

`file_get_contents`
file_get_contents関数は、ファイルの全体を読み込み、内容を文字列として返します。

`json_decode`
json_decode関数は、JSON形式の文字列を解釈して、PHPの文字列や配列といったデータに変換します。

`json_encode`
json_encode関数は、入力された値をJSON形式に変換します。

`is_uploaded_file`
is_uploaded_file関数は、変数に保存された一時的なファイルが、入力画面からアップロードされたファイルかどうかを調べます。

`mkdir`
mkdir関数はスクリプトがあるフォルダ以下に、指定したフォルダを作成します。

`basename`
アップロードされたファイルのファイル名に不正なフォルダ名などが含まれている不都合が生じるので、basename関数を使うことで、ファイル名だけを取り出します。

`move_uploaded_file`
アップロードされた一時的なファイルを、保存先のファイルに移動するために、move_uploaded_file関数を用います。

### 条件分岐
ユーザーの選択ないようなどによってスクリプトの流れを変化させる、<mark>制御構造</mark>という仕組み
条件に応じて処理を分岐させることを、<mark>条件分岐</mark>と呼びます。

#### 真偽値
真偽値というのは、条件の成立と不成立を表す値。真偽値にはtrueとfalseがあります。
条件が成立するときはtrue、成立しないときはfalseとなります。

#### if文による条件分岐
if文を使えば、条件が成立したときだけ実行する処理が書けます。

#### if-else文による条件分岐
if-else文を使えば、条件が成立または不成立によって行う処理を変えることができます。

#### switch文による条件分岐
switch文の式に値が一致するcase文が実行されます。
defaultは式の値に一致するcaseがないときに実行されます。

### ループ処理
スクリプトにおいて、指定した処理を指定した回数だけ繰り返したいことがあります。このような繰り返しのことを、<mark>ループ</mark>と呼びます。

繰り返し処理を行う場合の多くは、開始処理や更新処理を必要とします。このような場合には、開始処理や、更新処理を記述する場所があらかじめ用意されている、forループを使うのがおすすめです。
一方、開始処理や更新処理が不要なループについては、whileループを使うこともできます。

#### forループ
forループは開始処理、条件式、更新処理、繰り返し処理の４つで構成されていて、条件式がtrueの間は、処理を繰り返し行います。

#### whileループ
whileループは条件式がtrueの間は処理を繰り返します。

#### foreachループ
foreachループは配列に格納されたすべての値に対して繰り返し処理を行います。
キーと値を用いたforeachを記述することもできます。違いは、asの後を「変数」ではなく「キーの変数=>値の変数」とするところです。

##### 配列のキーと値
PHPの配列には、<mark>キー</mark>と<mark>値</mark>の組を格納する機能があります。

### データベース
データベースとは、データを集めたものです。そして、単にデータの集合ではなく、検索や更新といった処理が行いやすい形式に整理されています。
データベースの中でも、今日広く利用

#### データベース管理システム
コンピュータ上でデータベースを構築したり操作したりするためのソフトウェアのことを「データベース管理システム」と呼びます。英語ではdatabase management system、略してDBMSと呼びます。


### エラーメッセージ

## <form>について
### type属性
#### password
<input>タグのtype属性をpasswordにするとパスワード入力欄になります。

#### file
<input>タグのtype属性をfileにするとファイル選択欄を作成できます。

### name属性

### ファイルアップロードの時

## 正規表現
「.」は任意の１文字を表します。「\*」は直前の文字の０回以上の繰り返しです。両者を組み合わせた「.*」は、任意の文字を0文字以上繰り返すことを表します。
[]で囲まれた部分は文字クラスと呼ばれ、文字の集合を表します。「-」で範囲を表すことができます。例えば\[a-z]は、1文字の英小文字を表します。
\[0-9]は1文字の数字を表しますが、¥dという簡略な記法も用意されています。この記法を使うと、ハイフンなしの７桁の郵便番号を表す正規表現は^¥d{7}$のように書けます。
また、ハイフンありの場合は^¥d{3}-¥d{4}$です。

## セッションとは・・・

## SQLとは・・・

## SQLコマンド

## SQL用語
### 主キー

### 複合主キー

### ユニーク制約

### 外部キー制約

<!-- クガシオン作 -->
<!-- DO NOT CLONED -->